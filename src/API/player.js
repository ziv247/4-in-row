class Player {
    constructor(color) {
        this.color = color;
    }
    insertToken = (colIndex) => { return Boolean };
}

// class ComputerPlayer extends Player{
//     constructor(color){
//        super(color);
//     }
//     insertToken = (colIndex) =>{return Boolean};
// }

